# --- Core ML (pinned to avoid pulling NVIDIA/CUDA packages) ---
tflite-runtime
numpy<2
pillow
scikit-learn
joblib

# --- YOLO ---
ultralytics>=8.3.0
opencv-python-headless

# --- Web server ---
flask
gunicorn
python-dotenv
